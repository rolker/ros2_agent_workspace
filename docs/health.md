# Workspace health — 2026-09-21 14:03 -04:00

Generated by the `janitor-sweep` skill and replaced wholesale each run. The run's full record, including project-scope findings, is the local report under `.agent/scratchpad/janitor/` on the host that ran it.

## Workspace

| Check | Status | Detail |
|---|---|---|
| Workspace governance (`audit-workspace`) | FINDINGS | 7 of 7 sections completed; 2 stale worktrees holding unmerged work; ~20 scripts undocumented in the AGENTS.md table; 3 unreferenced templates. Principles-enforcement and ADR-accuracy sections were spot checks this run (2 of 10 principles, 1 of 19 ADRs) |
| Research-digest freshness | OK | `.agent/knowledge/research_digest.md` last updated 2026-09-17, 4 days (threshold 30); 17 entries, all stamped 2026-09-11, none past 90 days |

**First committed run — no prior health document.** Every finding below is `New` by definition; `Resolved` and `Unchanged` are empty because there is nothing committed to diff against, not because nothing changed. (A local, uncommitted sweep report from 2026-09-14 exists on the host that ran this; where it matters, a note below says what that run saw and this one did not re-examine.)

### 1. Work that can be lost
#### New
- `layers/worktrees/issue-s57_tools-23` (repo `s57_tools`): issue #23 closed 2026-08-20, but the worktree's last commit is 2026-04-25 and PR rolker/s57_tools#24 ("Closes #23") is still OPEN and untouched since then — 5 months idle, never merged; the issue was closed some other way.
- `layers/worktrees/issue-marine_perception_tools-4` (repo `marine_perception_tools`): issue #4 no longer resolves, PR rolker/marine_perception_tools#6 is CLOSED unmerged, last commit 2026-06-07 — remove the worktree or re-evaluate the work.
#### Resolved
- (none — first committed run)
#### Unchanged
- (none — first committed run)

### 3. Rules that have bitten with no enforcement
#### New
- (none found in this run's sample)
#### Resolved
- (none — first committed run)
#### Unchanged
- (none — first committed run)

**Not re-examined this run** (from the 2026-09-14 local report, full principles pass; absence here is not resolution): issue-closing keywords (`closes/fixes/resolves #N`) have no hook or CI check although AGENTS.md records two wrong-issue closures; the AI-signature requirement has no check; `validate.yml`'s required-documentation step checks only README.md and ARCHITECTURE.md; ADR-0005, ADR-0008 and ADR-0009 are still `Proposed` while AGENTS.md cites them as binding; the Consequences Map's ADR table omits 8 of 19 ADRs.

### 5. Drift
#### New
- AGENTS.md's Script Reference table documents 38 of ~58 non-internal scripts under `.agent/scripts/`; ~20 have no row (`bootstrap.sh`, `setup_layers.sh`, `list_overlay_repos.py`, `pr_status.sh`, `lock.sh`/`unlock.sh`, `framework_config.sh`, `discover_governance.sh`, `get_repo_info.py`, `read_feature_status.py`, `validate_repos.py`, `verify_change.sh`, `tmux_app.sh`, `test.sh`, `test_dispatch_subagent.sh`, `test_identity_introspection.sh`, `configure_git_identity.sh`, `detect_cli_env.sh`, `generate_make_skills.sh`, `build_report_generator.py`). No stale rows: every documented script exists.
- Three templates in `.agent/templates/` are referenced nowhere in AGENTS.md, CLAUDE.md, `.agent/knowledge/` or `.claude/skills/`: `pr_template.md`, `improvement_issue.md` (names a "Continuous Improvement workflow" that is itself unreferenced), `ai-review.yml`.
#### Resolved
- (none — first committed run)
#### Unchanged
- (none — first committed run)
