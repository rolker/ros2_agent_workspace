# Agent image build-context excludes (#520).
# The image needs only the Dockerfile, the entrypoint, and the staged layer
# manifests (.rosdep-manifests/, generated by docker_run_agent.sh --build for
# the rosdep bake). Keep docs and editor cruft out of the context upload.
README.md
.DS_Store
**/.DS_Store
