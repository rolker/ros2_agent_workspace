# Workspace dev-tool dependencies — installed into .venv by `make lint`.
# Keep minimal: only tools wired into Makefile targets or CI.
pre-commit
playwright  # browser tests — also run: .venv/bin/playwright install chromium
