{"author":{"id":"b78c39e7b13f90a5827abfba043255fe7a4ede1499df23f110fffe5abfa44adc"},"ops":[{"type":5,"timestamp":1772071106,"nonce":"tyRwo1xcRzj4Qc1h84xg6b8YtCw=","metadata":{"github-id":"LE_lADOQ241x87t_V9YzwAAAAVgI9W_"},"added":["enhancement"],"removed":[]},{"type":3,"timestamp":1772206540,"nonce":"/TP8MPo5qJ2l/ZQEyEkgV33ixd4=","metadata":{"github-id":"IC_kwDOQ241x87s2Cvk","github-url":"https://github.com/rolker/ros2_agent_workspace/issues/263#issuecomment-3973589988"},"message":"I would like to rename the env script to setup to mimic ros workspaces.","files":null},{"type":3,"timestamp":1772458257,"nonce":"FG8LgpsnQkHB7anB5f85gKg1iE0=","metadata":{"github-id":"IC_kwDOQ241x87tfVPa","github-url":"https://github.com/rolker/ros2_agent_workspace/issues/263#issuecomment-3984413658"},"message":"## Review\n\n### Scope Assessment\n\n**Well-scoped?** Mostly — but bundles four distinct workstreams (audit, tooling decision, consolidation, self-documenting interface). The Makefile vs. `just` decision is particularly consequential; it could warrant its own ADR issue rather than being resolved inside this one. Consider whether steps 1 and 2 are a first PR, and steps 3 and 4 follow.\n\n**Owner comment**: Renaming `env.sh` → `setup` is significant scope (3 framework adapter files + `AGENTS.md` script table need updating). Worth capturing as an explicit sub-task or linked sub-issue so it doesn't get missed.\n\n**Right repo?** Yes — this is workspace infrastructure, correctly placed in the workspace repo.\n\n**Dependencies**: Issue #252 (script intent recovery — complete ✅) and Issue #249 (research — available ✅). No blockers.\n\n---\n\n### Principle Alignment\n\n| Principle | Status | Notes |\n|---|---|---|\n| Only what's needed | ✅ OK | Issue directly serves this principle — reducing two-layer indirection and dead code |\n| Improve incrementally | ✅ OK | Issue explicitly says \"can be done in stages\"; scope items are ordered |\n| Human control and transparency | ✅ OK | Self-documenting interface (`make help` / `just --list`) improves transparency for both humans and agents |\n| A change includes its consequences | ⚠️ Watch | AGENTS.md script reference table, `make generate-skills` for slash commands, and framework adapters are not mentioned as things to update (see Consequences below) |\n| Enforcement over documentation | ⚠️ Watch | The `env.sh` checkout guardrail (blocks `git checkout`) is enforcement, not just documentation. Any rename or refactor of that script must preserve the guardrail behaviour — this isn't called out in the issue |\n| Capture decisions, not just implementations | ⚠️ Action needed | The Makefile vs. `just` choice is a significant tooling decision that should be recorded as an ADR (ADR-0001 is triggered) |\n\n---\n\n### ADR Applicability\n\n| ADR | Triggered | Notes |\n|---|---|---|\n| 0001 — Adopt ADRs | ✅ Yes | The tooling decision (keep Makefile or migrate to `just`) needs an ADR so future agents don't re-litigate it |\n| 0002 — Worktree isolation | ✅ Yes | Applies to implementation (use a worktree, not a branch switch) |\n| 0003 — Project-agnostic workspace | ✅ Yes | All changes must remain generic ROS 2 infra — no project-specific content |\n| 0004 — Enforcement hierarchy | ⚠️ Watch | If enforcement scripts (especially `env.sh`) are renamed or restructured, the multi-layer enforcement chain must remain intact |\n| 0005 — Layered enforcement | ⚠️ Watch | Same as above — `env.sh` is part of the enforcement layer; its guardrail must survive refactoring |\n| 0006 — Shared AGENTS.md | ✅ Yes | Changing scripts or Makefile targets triggers an update to `AGENTS.md` and possibly all three framework adapters |\n\n---\n\n### Consequences\n\nThe issue doesn't mention these, but they must be included in scope (or explicitly deferred as follow-up issues):\n\n- **AGENTS.md script reference table** — must be updated for any added, removed, or renamed scripts\n- **Framework adapters** — `CLAUDE.md`, `.github/copilot-instructions.md`, `.agent/instructions/gemini-cli.instructions.md` all hardcode `source .agent/scripts/env.sh`; renaming that script requires updating all three\n- **`make generate-skills`** — any change to Makefile `.PHONY` targets requires regenerating `.claude/skills/make_*/SKILL.md` slash commands\n- **ADR** — tooling decision (Makefile vs. `just`) needs a `docs/decisions/` entry\n- **`AGENTS.md` build/test commands** — if `just` is adopted, the documented build commands change\n\n---\n\n### Recommendations\n\n1. **Rename `env.sh` to `setup`** (per owner comment) as an explicit sub-task, with a checklist of files to update (AGENTS.md, CLAUDE.md, copilot-instructions.md, gemini-cli.instructions.md). This is mechanical but easy to miss.\n2. **Write the ADR first** — before committing to `just` or staying with Make, record the decision. This also keeps the PR focused: the ADR PR = the decision; the follow-up PR = the implementation.\n3. **Scope the first PR narrowly** — the audit (step 1) plus dead-code removal is a clean, low-risk first PR. The tooling decision and large-scale consolidation can follow.\n4. **Note the principle name change** — the issue references \"Principle #5: Radical simplicity\" from a feature branch URL; the current name in `docs/PRINCIPLES.md` is **\"Only what's needed\"**.\n\n---\n**Authored-By**: `Copilot CLI Agent`\n**Model**: `claude-sonnet-4.6`","files":null},{"type":3,"timestamp":1772458855,"nonce":"rQCC6pindI9FGg0xSm0Ops7Tznw=","metadata":{"github-id":"IC_kwDOQ241x87tfk4F","github-url":"https://github.com/rolker/ros2_agent_workspace/issues/263#issuecomment-3984477701"},"message":"Two sub-issues have been opened to address the most consequential dependencies before this issue proceeds:\n\n- **#324** — [Rename env.sh to setup.sh to mirror ROS workspace conventions](https://github.com/rolker/ros2_agent_workspace/issues/324) (per owner request in this thread)\n- **#325** — [ADR: Makefile vs just as workspace task runner](https://github.com/rolker/ros2_agent_workspace/issues/325) (tooling decision must be recorded before large-scale consolidation)\n\n**Next step**: Once #324 and #325 are resolved, we'll re-evaluate how to proceed with the remaining scope here — consolidating entry points, removing dead code, and ensuring the interface is self-documenting.\n\n---\n**Authored-By**: `Copilot CLI Agent`\n**Model**: `claude-sonnet-4.6`","files":null}]}